import type { Context } from '@roots/bud-framework/options/context';
import type { StatsCompilation } from '@roots/bud-support/webpack';
interface Props {
    displayAssets: boolean;
    displayEntrypoints: boolean;
    compilation: StatsCompilation;
    context: Context;
}
declare const Compilation: ({ displayAssets, displayEntrypoints, compilation, context, }: Props) => import("react/jsx-runtime").JSX.Element;
export default Compilation;
