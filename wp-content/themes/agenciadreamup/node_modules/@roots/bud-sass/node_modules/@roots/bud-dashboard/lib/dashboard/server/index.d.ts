interface Props {
    devUrl?: URL;
    publicDevUrl?: URL;
    watchFiles: Set<string>;
    displayServerInfo: boolean;
    proxyUrl?: URL;
    publicProxyUrl?: URL;
}
/**
 * Server info ink component
 */
export declare const Server: ({ devUrl, publicDevUrl, displayServerInfo, watchFiles, proxyUrl, publicProxyUrl, }: Props) => import("react/jsx-runtime").JSX.Element;
export {};
